# Default values for helm-basic-example.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

deployment:
  replicaCount: 1
  image:
    repository: nginx
    tag: latest

service:
  type: ClusterIP
  port: 80

